package org.mycorp.models.events.commcsmshelper;

import org.mycorp.models.events.StationEvent;

public class TryConnecting extends StationEvent {
    public TryConnecting(Object source) {
        super(source);
    }
}
