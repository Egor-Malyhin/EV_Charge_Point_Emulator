package org.mycorp.models.events.common;

import org.mycorp.models.events.StationEvent;

public class StopChargingEmergency extends StationEvent {
    public StopChargingEmergency(Object source) {
        super(source);
    }
}
